// 1. get url

// 2. look up url in urls (from datastore)

// 3. serve 404 if not found

// 4. if found, load corresponding instance and render

var Tipi = new Tipi;
Tipi.createItemFromUrl();